# experimental scripts

这里放还没接成比赛主流程的实验入口。

`run_agent.py` 目前只是 LLM tool-calling 骨架，会直接提示 TODO；比赛时请使用：

```bash
python scripts/run_scripted.py --prompt "a bottle" --arm right --strategy default --speed SLOW
```
